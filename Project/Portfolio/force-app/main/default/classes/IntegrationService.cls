public class IntegrationService {

    public static void syncCases(List<Case> casesToSync) {
        // PoC placeholder
        // Future: REST callout using Named Credentials
        System.debug('Syncing cases: ' + casesToSync.size());
    }
}
